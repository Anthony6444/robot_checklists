import 'package:flutter/material.dart';

class PageLadderDrivers extends StatelessWidget {
  const PageLadderDrivers({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement drivers page
    return const Center(
      child: Text("Drivers"),
    );
  }
}
